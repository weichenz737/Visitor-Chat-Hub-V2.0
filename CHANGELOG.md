# Changelog

## V2.3.1 — 2026-07-13

### 访客端移动体验
- 修复 iOS / 微信扫码打开后输入框被顶到屏幕上方（改用 `100dvh` + 键盘 inset，不再用 `visualViewport.height` 当整页高度）
- 含图片历史首次进入对齐到最新消息；图片懒加载（进屏再鉴权拉取）
- 发送文本后保持键盘不收起
- 优化上滑浏览历史时的抖动（取消错误贴底回拉；键盘 inset 仅在输入聚焦时生效）

### 客服端移动体验
- 发送后保持键盘；上滑查看历史时仅访客新消息显示「有新消息」
- 转接成功后立即从列表移除该访客，并返回会话列表、退出会话房间
- 修复转接后仍能打开已转出会话、看到其他客服对话的问题

### 后端 / 实时
- 进行中会话列表仅包含当前 WAITING/ACTIVE 接待或可接入会话，不再因历史 CLOSED 会话残留
- 会话消息访问权限同步收紧（归档会话仍可查看）
- 新增 WebSocket `leave_session`，转出后不再接收该会话实时消息

### SDK
- Widget 发送文本后同样保持输入焦点

## V2.3 — 2026-07-12

### 客服工作台移动端
- 窄屏改为「会话列表 ↔ 聊天」单栏导航，右侧能力收入「更多」底部 Sheet
- 接入 PWA（manifest / Service Worker），支持添加到主屏幕
- 修复移动端进会话白屏、虚拟列表重复加载、Sheet 内容漏出等问题

### 登录与账号
- 客服 / 平台 / 企业登录页：首次不再预填演示账号
- 支持「记住密码」；企业编码登录成功后自动记住并回填
- 登录态写入 localStorage，刷新页面保持已登录
- 演示账号调整：
  - 平台管理员：`admin` / `admin123`
  - 企业管理员：`demo` / `123456`（企业编码 `demo001`）

### 其他
- 客服端安全区 / `100dvh` / 输入框 16px 等移动端体验优化
